%div{style: "overflow-y: scroll; height: 800px"}
  %h1 Результаты игровых дней
  
  - @days.each do |day|
    %h4{align: 'center'}= day.date.strftime("%-d %B, %A")
    %p{align: 'center'}= day.comment
    %table#games{width: 600, :class => "table table-condensed table-hover"}
      %thead
        %tr
          %th №
          %th Winner 1
          %th Winner 2
          %th Loser 1
          %th Loser 2
          %th W pts
          %th L pts
      %tbody
        - day.games.each do |game|
          %tr
            %td= game.num_in_day
            %td= first_winner(game.results).player.name
            %td= second_winner(game.results).player.name
            %td= first_loser(game.results).player.name
            %td= second_loser(game.results).player.name
            %td= game.win_points
            %td= game.lose_points
  
  = link_to 'На главную', '/main/main'
